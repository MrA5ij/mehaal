<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - MEHAAL Admin</title>
  <link rel="stylesheet" href="/admin.css">
</head>
<body>
  <div class="login-container">
    <div class="logo">MEHAAL Admin</div>
    <% if (error) { %>
      <div class="alert"><%= error %></div>
    <% } %>
    <form method="POST" action="/admin/login">
      <%- include('../partials/form-group', { id: 'username', label: 'Username', name: 'username', type: 'text', required: true }) %>
      <%- include('../partials/form-group', { id: 'password', label: 'Password', name: 'password', type: 'password', required: true }) %>
      <%- include('../partials/button', { label: 'Login', type: 'submit' }) %>
    </form>
  </div>
</body>
</html>
